#VLSI FINAL PROJECT, FALL 2022
#CHRISTOPHER LUEVANO , ANDREW SZYMANSKI, JESSE THOMPSON

Installation:

Make sure the libraries 'multipledispatch' and 'z3 solver' are installed. This can be done with console commands:

	pip install multipledispatch
	pip install z3-solver



Execution:

The main file to run is driver.py